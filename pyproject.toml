[tool.black]
line-length = 120
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | _build
  | build
  | dist
  | elasticapm/utils/wrapt

  # The following are specific to Black, you probably don't want those.
  | blib2to3
  | tests/data
)/
'''
